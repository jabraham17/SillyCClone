
FUNC foo;
PARAM x, y, z;
VAR a, b, c, c9, m;
{

    a = a + b + c;
    x = 7;
    WHILE (x +y == m-z-z) {
        x = 0;
    }
    m = 8;
    IF(m==0) {
        CALL foo();
    }
}

FUNC test;
PARAM x, y, z;
VAR a, b, m;
{
    a= a+b;
    m = a+b;
    
    x = 7;

    IF(x == y) {
        x = 0;
        y = 0;
    }

    a = 18;
    CALL foo();

    y = CALL foo(x,y,z);

   RETURN x==7;
}

/*
FUNC bar;
{
    VAR lamo;
    lamo = 8;
    CALL t();
    CALL m(x, y, z, a);
    IF (x) RETURN m;
    
    IF(m) ;
    {
        x = 6;
    }
    RETURN lmao+7;
}
*/

